@model IEnumerable<landingpagemaker.Models.LandingPageStat>

@{
    
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LandingPage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalVisits)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalActions)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LandingPage.Content)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalVisits)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalActions)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
